import { HOSTS_IMAGE } from '@/lib/hosts'

/**
 * Placeholder artwork for topics that have not been generated yet.
 * Uses the ClearSight hosts studio image so ungenerated topics still feel
 * branded and familiar (Dr. Anderson + Sarah Chen).
 */
export const UNGENERATED_BRIEFING_PLACEHOLDER = HOSTS_IMAGE
