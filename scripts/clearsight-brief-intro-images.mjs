/**
 * Curated Brief intro frame images — re-export from app bundle (single source of truth).
 */
export {
  CLEARSIGHT_BRIEF_INTRO_FRAME_IMAGES,
  applyBriefIntroFrameImages,
} from '../src/lib/clearsight-brief-intro-images.ts'
